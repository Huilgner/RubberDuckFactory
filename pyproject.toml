[project]
name = "rubberduckfactory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "chromadb>=1.5.9",
  "sentence-transformers>=5.4.1",
  "httpx>=0.27",
  "python-dotenv>=1.0",
  "qdrant-client>=1.18.0",
  # Teto obrigatorio: no mcp 2.x o FastMCP virou MCPServer e o import
  # `from mcp.server.fastmcp import FastMCP` deixa de existir. server.py e
  # quality_gate_server.py ainda usam a API v1. Migracao pendente antes de
  # levantar este teto.
  "mcp>=1.27.1,<2",
]

[dependency-groups]
dev = [
  "pytest>=8.0",
]
